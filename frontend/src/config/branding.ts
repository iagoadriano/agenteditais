// Identidade do produto — ponto único de rebrand.
// Para renomear o sistema, defina VITE_APP_NAME no ambiente de build
// (ex.: VITE_APP_NAME="MeuNome.IA" npm run build) ou altere o default abaixo.
export const APP_NAME: string = import.meta.env.VITE_APP_NAME || "Facilicita.IA";

export const APP_TAGLINE: string =
  import.meta.env.VITE_APP_TAGLINE || "Gestão inteligente de licitações públicas";
